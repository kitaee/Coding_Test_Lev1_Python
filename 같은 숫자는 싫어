def solution(arr):
    answer = []
    answer.append(arr[0])
    for k in range(1,len(arr)):
        if(arr[k]==answer[-1]):
            continue
        else:
            answer.append(arr[k])
    return answer
