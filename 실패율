def solution(N, stages):
    result={}
    user=len(stages)
    for k in range(1,N+1):
        if user!=0:
            count=stages.count(k)
            result[k]=count/user
            user-=count
        else:
            result[k]=0
    return sorted(result,key=lambda x:result[x], reverse=True)
